{
  "pacman": {
    "depends": {
      "systemd": ">=244.3"
    }
  },
  "template": {
    "usedBy": [ "arlac77/npm-package-template-kronos-app" ],
    "files": [
      {
        "merger": "INI",
        "pattern": "**/*.service",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      },
      {
        "merger": "INI",
        "pattern": "**/*.socket",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      },
      {
        "merger": "INI",
        "pattern": "**/*.mount",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      }
    ]
  }
}
