{
  "pacman": {
    "depends": {
      "systemd": ">=245"
    }
  },
  "template": {
    "usedBy": [
      "arlac77/hook-ci",
      "arlac77/template-kronos-app"
    ],
    "mergers": [
      {
        "type": "INI",
        "pattern": "**/*.service",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      },
      {
        "type": "INI",
        "pattern": "**/*.socket",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      },
      {
        "type": "INI",
        "pattern": "**/*.mount",
        "options": {
          "messagePrefix": "fix(systemd): ",
          "expand": false
        }
      }
    ]
  }
}
